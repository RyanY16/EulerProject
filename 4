biggest = 0
for i in range(100, 999):
    for j in range(100, 999):
        number = str(i*j)
        if number == number[::-1]:
            if int(number) > int(biggest):
                biggest = number
print(biggest)
